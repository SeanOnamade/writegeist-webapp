import React from 'react';
import { ChapterEditor } from './ChapterEditor';

// Note: Window.api interface is extended in preload.ts

export const ChapterIngest: React.FC = () => {
  return <ChapterEditor />;
}; 